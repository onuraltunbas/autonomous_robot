[package]
name = "webots_ros2_msgs"
version = "2025.0.1"
edition = "2021"

[dependencies]
rosidl_runtime_rs = "0.6"
serde = { version = "1", optional = true, features = ["derive"] }
serde-big-array = { version = "0.5.1", optional = true }

# ROS Dependencies
builtin_interfaces = "*"
geometry_msgs = "*"
service_msgs = "*"
std_msgs = "*"
vision_msgs = "*"

[features]
serde = ['dep:serde', 'dep:serde-big-array', 'rosidl_runtime_rs/serde', 'builtin_interfaces/serde', 'geometry_msgs/serde', 'service_msgs/serde', 'std_msgs/serde', 'vision_msgs/serde']

[package.metadata.ros-env]
include = true
